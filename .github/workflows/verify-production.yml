name: Verify Production (headers/redirects/seo)

on:
  push:
    branches: [ "main" ]
  workflow_dispatch: {}

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify live site
        run: bash scripts/verify-prod.sh
